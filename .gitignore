# decompiled source of mods for comparison, never commit
/references

# Gradle / build output
/build
/.gradle
/run

# Local-only jars used to compile against mods with no Maven artifact (e.g. Spore) - never commit
/libs/*
!/libs/.gitkeep

# IDE
/.vscode
/.idea
*.iml